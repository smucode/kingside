define(['underscore'], function(_) {
	console.log(_);
	
    // click foo, bar
    
    // new game

    var board = board;

	var white = playerFactory.create('garbo', 'w', board);
	
	var black = playerFactory.create('local', 'b', board);
	
	   return new local

	       update
	           board.update
           
           board.onMove
               check color 
                   this.onMove


	new game(white, black);

	// var black = playerFactory.create('remote');
	
	var Game = function({w, b}) {
	    var rex = new;
	    
	    w.onmove
	       rex.move

        b.onmove
            rex.move
	    
	    rex.onmove
	       w.update
	       b.update
	};
	
	// board
	
	ctor w, b:
	   
	   if b.type == 'local'
	       orientation = b;
	   
	   if w.type == 'local'
	       orientation = w; 
	   
       isBlackLocal
            to determine if move is legal

       isWhiteLocal	   
            to determine if move is legal

	// list games
	
	var games = [];

    each games.sort -> game
        print game
        
    onclick
        get game
        containers[game] -> show
        hide list

    // 

	return 'bar';
});
